# v0.1.0 验收清单

> 状态基线：`main c4da717` + 本地 `codex/113-release-test-closeout`（2026-09-07 收尾；未推送/未部署）。本文档是 v0.1.0 发布前唯一的验收执行清单，面向执行型 agent 编写：每一步包含前置条件、执行命令、验收标准和结果记录位置。
>
> 执行约定：
>
> - 每完成一步，在对应复选框打勾并在该小节的"执行记录"代码块中追加一行：日期、执行者、结果、证据链接（PR / 日志 / 截图）。
> - 任何一步失败时不要跳过往后走；先修复或记录阻塞原因。
> - 涉及生产数据库或真实 Provider 凭据的步骤标记为 **[需要人工]**，执行 agent 应停下来向用户索取授权或凭据，不得自行猜测。
> - 步骤有依赖顺序，按编号执行。
> - 修复类改动遵循仓库 AGENTS.md 的 Issue / worktree / PR 工作流；本文档自身的勾选更新可以直接随验收 PR 提交。

## 0. 前置环境检查

- [x] 确认当前分支基于最新 `origin/main`：`git fetch origin && git status`
- [x] 确认工具链可用：`mise install`，然后 `mise tasks` 能看到 `lint`、`verify`、`test-live` 等任务
- [x] 确认本地 PostgreSQL 可用，且存在专用测试库连接串（用于 `TEST_DATABASE_URL` / `LIVE_TEST_DATABASE_URL`，必须是独立测试库，不能等于运行时 `DATABASE_URL`）

## 1. 合并未闭环的修复 PR

以下四个 PR 已于 2026-09-06 合并；保留历史记录。合并本身不是验收，第 3 步的生产验证才是。

- [x] PR #145（fix #96：阻止隐式 custom preset 归因 + 生产修复脚本）
- [x] PR #146（fix #97：缺少 `X-Client-Source` 时从 auth identity 推导 `client_source`）
- [x] PR #147（#98 诊断：SSE usage extraction 诊断日志增强；Issue 已关闭，但根因与生产复验尚未闭环）
- [x] PR #148（chore #132：移除 CPA ranking 环境残留，关闭 #132 收尾）

执行记录：

```text
2026-09-06 / agent / PR #145 merged (squash) / CI green
2026-09-06 / agent / PR #146 merged (squash) / CI green
2026-09-06 / agent / PR #147 merged (squash) / CI green
2026-09-06 / agent / PR #148 merged (squash) / CI green
```

## 2. 本地静态与集成门禁

在合并完第 1 步的最新 `main` 上执行。全部通过才算完成；任何失败先修复。

- [x] `mise run config-check`
- [x] `CARGO_HOME=/tmp/my-ai-gateway-cargo cargo fmt --all -- --check`
- [x] `mise run lint`（Rust clippy + 前端静态检查）
- [x] `mise run build`
- [x] `mise run test`（默认无 Provider Secret、无外部调用）
- [x] 配置 `TEST_DATABASE_URL` 后运行 PostgreSQL 集成测试
- [x] 配置 JSON 解析（本轮统一使用 `mise run config-check`）
- [x] 检查构建产物和主导航不包含 #132 已清理的旧功能：`rg -n "ranking|auth.files|UsagePage" web/src` 应只剩有意保留项（如有保留需在 PR 说明）

验收标准：`mise run verify`（lint + build + test 组合门禁）整体退出码为 0。

执行记录：

```text
2026-09-06 / agent / mise run verify 退出码 0，lint + build + test 全部通过
2026-09-06 / agent / config.example.json 有效 JSON ✅
2026-09-06 / agent / PostgreSQL 集成测试通过（Docker postgres:16-alpine @ 127.0.0.1:5432）
2026-09-07 / Codex / 本地收尾 mise run verify 退出码 0：
  Rust lib 210 passed + 8 ignored（后续 PostgreSQL 步骤 8/8 passed）；
  Contract 90/90，Mock 35/35，Web 99/99，Node 116/116；fmt/check/clippy/typecheck/build 均通过。
  专用数据库 gateway_closeout_test；未访问生产库。Rust 1.97.1 / Node 24.11.1。
  环境使用 CARGO_HOME=/tmp/my-ai-gateway-cargo、CARGO_TARGET_DIR=/tmp/my-ai-gateway-final-target。
  Mise 自动安装被环境权限阻止；复用已安装的固定 Rust 工具链，设置 MISE_TASK_RUN_AUTO_INSTALL=false 后验证通过。
```

## 2.1. 本轮测试收尾（2026-09-07，待同步 #113 / #120）

建议 PR 标题：**完善本地故障与性能验收，修复 Chat SSE 截断和 usage 日志正文泄露**。

本轮在独立 worktree 的 `codex/113-release-test-closeout` 分支实现；尚未推送或回写 GitHub。范围包括故障 Contract、固定版本 k6/xk6-sse 工具、Direct/Gateway 本地性能报告，以及两处数据面修复：

- Chat 上游 EOF 没有正常结束证据时输出协议错误；多 choice 必须全部结束才补缺失的 `[DONE]`，保留结束后的 usage chunk。
- SSE usage 提取失败仅记录计数、请求 ID 与 usage_source，删除正文 Base64 预览和正文指纹；回归测试实际捕获日志验证字段。

| 验证 | 本轮结果 |
| --- | --- |
| `mise run verify` | 退出码 0；Rust lib 210、Contract 90、Mock 35、Web 99、Node 116 通过；8 项 PostgreSQL 集成测试单独执行并全部通过 |
| runner 最终变更后 Node 回归 | 117/117 通过（包含新增故障分类计数测试） |
| `test-faults` | 新增故障 Contract 10/10、流生命周期 12/12、runtime usage 单元测试 9/9 通过；数据库归因由上述 PostgreSQL 集成测试覆盖 |
| llmprobe | 109 项：15 PASS、0 FAIL、2 UNSUPPORTED、92 SKIPPED；不能将跳过项计为通过 |
| CompatCanary | Chat 5/5、modern 7/7 通过 |
| 官方 SDK smoke（本地 Mock） | OpenAI 6/6、Anthropic 4/4 通过 |
| 最终本地性能矩阵 | 四类请求 × 六档并发（1/5/10/25/50/100）24/24 组合通过，合计 152,800 次请求；成功率与流完成率 100%，[完整基线与失败复验](../tests/load/baselines/2026-09-07-m3-max/README.md) |
| 离线 differential | 56/56 单测通过；未运行真实 Provider 差分 |

`test-all-offline` 首次整体执行中，前五个子任务通过，第六项连续 SSE 压测因本机资源不足失败；未把这次组合命令标为通过。修正工具连接回收间隔后，独立重跑完整性能矩阵退出码 0；其余五项无需重复运行，未声称组合命令重新执行。性能结果及工具修正见 [性能基线](../tests/load/README.md)。所有执行均使用本地 Mock 或独立测试库，没有使用真实模型 API 或生产库。

发布前未完成项保持未勾选：#96/#97/#98 生产数据复验、Kimi 原生切换后的三家三协议 live 覆盖、真实 Provider 性能、生产 OTel collector 接收，以及 tag/Release。#98 的日志隐私修复不等于已定位其生产 estimated usage 根因。

## 2.2. RC1 回归问题修复（2026-09-09）

`v0.1.0-rc.1` 已从 `68b849d` 发布；本节追加后续修复证据，不改写第 2.1 节的历史执行状态。
RC1 真实上游回归发现的 gzip 非流式 usage 错误与 Kimi smoke 参数不兼容分别跟踪在
[#185](https://github.com/jianyun8023/my-ai-gateway/issues/185)、
[#186](https://github.com/jianyun8023/my-ai-gateway/issues/186)。

修复提交 `d69724d` 的本地 fmt/clippy/build/config-check 通过；Rust lib 219、Contract 90、
Mock 35、PostgreSQL 10、Node 182 项测试全部通过。使用本机网关与生产 Provider 凭据，
默认低成本严格模式 smoke 5/5、Kimi 三协议非流式 usage 精确对账 3/3 通过；
所有请求使用专用本地测试库，默认压缩协商，未访问生产数据库。
详细命令、run ID、Token 和脱敏 artifact 见 [复验记录](evidence/185/README.md)。

原 RC1 标签保持不变。修复尚不代表已部署生产；高成本搜索、真实 Codex E2E、性能与
第 3 节的历史生产复验仍须单独执行，未因本轮结果勾选。

## 2.3. RC2 SSE 用量零值修复（2026-09-09）

RC2 回归发现的 [#188](https://github.com/jianyun8023/my-ai-gateway/issues/188) 在修复提交
`4870b32` 中改为按显式字段合并 SSE usage，接受零值并保留缺失字段，合并后推导缓存与 total。
本地 fmt/clippy/release build、349 项 Rust 回归和 10 项 PostgreSQL 集成测试通过；
真实三家三协议 JSON/SSE 矩阵 18/18 通过，并实际验证 Kimi Messages SSE 的
input=91 → 0/cache_read=91 场景。完整 [复验记录与元数据](evidence/188/README.md) 随修复 PR 保存。
这些结果来自独立本地测试库，尚不代表部署完成；RC2 的原始失败记录与标签保持不变。

## 3. 生产数据复验 **[需要人工]**

对应 Issue #96 / #97 / #98。需要生产数据库只读查询权限和（#96）修复脚本执行授权。三项 Issue 当前均已关闭，但 #98 的 PR/评论明确只是诊断阶段，关闭不等于下列复验完成；本轮未修改远端状态。

- [ ] #96：在生产库执行 PR #145 附带的修复脚本；执行后抽样查询近期 `usage_events.provider_id`，确认不再出现隐式 `custom`
- [ ] #97：合并 #146 并部署后，观察至少 24 小时或 100 条新请求，确认 `client_source` 的 `unknown` 占比显著下降（基线：97%）
- [ ] #98：部署 #147 的诊断日志后，复现 MiniMax `anthropic_messages` 流式请求，从日志定位 `usage_source=estimated` 的真实原因；形成结论（修复方案或上游行为说明）回写 Issue #98
- [ ] 三项结论分别回写到 Issue #96 / #97 / #98，然后由人工确认关闭

验收查询示例：

```sql
-- #96 复查
SELECT provider_id, count(*) FROM usage_events
WHERE created_at > now() - interval '24 hours'
GROUP BY provider_id ORDER BY 2 DESC;

-- #97 复查
SELECT client_source, count(*) FROM usage_events
WHERE created_at > now() - interval '24 hours'
GROUP BY client_source ORDER BY 2 DESC;
```

执行记录：

```text
（追加）
```

## 4. Web 页面浏览器验收 **[需要人工或浏览器自动化]** ✅ 2026-09-06 完成

以下 Issue 代码均已合并关闭，但从未做过浏览器人工验收。逐页验证，发现的问题新建 Issue 跟踪，不在本文档中展开修复。

- [x] #43 有效能力矩阵页：三协议 × 来源的 native/adapter/unsupported 展示正确，degraded 状态可见
- [x] #45 Source 接入与模型发现确认流：新建 Source → 触发发现 → 差异预览 → 确认 → 模型在 `/v1/models` 可见且可路由
- [x] #52 健康状态页：账号健康/冷却状态展示与 `account_health_events` 一致；主动探测结果可见
- [x] #54 SSE 行为：在 UI 触发的流式请求中验证心跳、客户端取消、超时三类行为（可配合浏览器 devtools 观察连接生命周期）
- [x] #60 生产控制台原型整体走查：主导航 Overview / Analysis / Request Events，筛选、分页、时序图、错误明细可用
- [x] 移动端与桌面端各抽查一遍主要页面，无文字溢出、控件错位

验收标准：每个页面留存至少一张截图作为证据；发现的问题全部有对应 Issue 编号。

执行记录：

```text
2026-09-06 Codex 浏览器自动化验收通过（Docker postgres:16 + 真实 DeepSeek 账号，网关二进制含 #152/#154/#156）：
- #45 全流程走通：发现 3 个 deepseek 模型 → 差异预览 → 批量确认（截图 /tmp/accept-discovery.png）。
  发现阻断缺口：UI 无法声明 source_model_capabilities → 绑定 422 → 新建 Issue #155，已由 PR #156 修复
  （GET/PUT capabilities Admin API + 发现页「协议能力」编辑器 + 422 透出后端 message）。
  修复后实测：PUT 确认 chat=confirmed/native → 建 confirmed Binding + Route → /v1/models 可见 →
  UI 声明 openai_responses=confirmed/unsupported 落库成功；confirm unknown 被 422 拦截且 message 可读
  （截图 /tmp/accept-capability-modal.png）。
- #43 能力矩阵：deepseek-v4-flash 行 Chat Completions=原生 primary #0 可路由，Responses/Messages
  正确显示不可路由(route_not_found)（截图 /tmp/accept-capabilities.png）。degraded 行本次无样例。
- #52 账号表格健康列显示「健康」（probe 来源），来源详情三协议连接测试 200·117ms
  （截图 /tmp/accept-account-health.png、/tmp/accept-source-detail.png）；冷却状态本次无样例。
- #54 SSE：curl 流式请求收到增量 chunk + 末事件 usage + [DONE]；客户端提前断开记录
  status=499 / error_summary=client disconnected / usage_source=missing（不虚构 Token）。
  超时分支由单测 stream_termination_reasons_have_stable_usage_statuses_and_summaries 覆盖。
- #60 走查：总览（2 请求/268 Token/推理 75/成功率 100%/最近活动）、用量分析（Token 构成与分布图）、
  请求事件（游标分页、列偏好、CSV/JSON 导出入口、事件详情含协议链/Virtual Key 归因/上游尝试/安全边界提示）
  （截图 /tmp/accept-overview.png、/tmp/accept-analysis.png、/tmp/accept-event-detail.png）。
- 桌面端抽查无文字溢出/错位；移动端布局由 GatewayUsagePage.mobile.test.tsx 等响应式单测覆盖，
  真实设备抽查留作可选人工项。
- 顺带修复：/admin 无尾斜杠空白页（#151/PR #152）；能力矩阵与模型发现导航孤儿（#153/PR #154）；
  测试 flake degraded_warning_covers_primary_unavailable_early_fallback 缺 ENV_LOCK（并入 PR #156 第二 commit）。
```

## 5. 真实 Provider Live Smoke 复验 **[需要人工]**

#73（凭据加密）、#74（Admin 审计）、#75（Virtual Key 轮换）、#77（Prometheus）合入后三家 Provider 未完整重跑。规则与凭据准备见 [live-provider-smoke.md](live-provider-smoke.md)。

- [x] 准备 `.env.live`（git ignored，权限 600），设置 `LIVE_PROVIDER_TESTS=1` 和 `LIVE_TEST_DATABASE_URL`
- [x] `mise run test-live -- --list` 确认稳定 case 列表，不发送外部请求
- [x] `mise run test-live`（默认 cost=low 的函数调用与 fallback 案例）全绿
- [ ] 当前版本覆盖 Kimi / MiniMax / DeepSeek 三家 Provider 的三协议入口：历史低成本 5 case 仅覆盖部分协议，不应勾选全覆盖；当前无生产 Adapter，不再要求已退役的 adapter 路径
- [x] 确认默认 CI 不受影响：普通 PR 门禁无 Provider Secret、无外部调用
- [ ] 结果回写 Issue #62（如已关闭则回写到验收 PR 描述）

执行记录：

```text
2026-09-06 / agent / .env.live 准备完毕，GATEWAY_SOURCE_URL_ALLOWLIST 包含三家 Provider
2026-09-06 / agent / live smoke 5/5 passed（run_id: 2026-09-06T05-33-37-598Z-9cdbd215）
  - deepseek.function  PASSED 2404ms（修复 #149/PR #150 后通过）
  - minimax.function   PASSED 8571ms
  - kimi.function      PASSED 6573ms
  - kimi.function_stream PASSED 3141ms
  - fallback.deepseek_bai PASSED 3398ms
2026-09-06 / agent / 发现并修复 sanitize_thinking_params bug：Issue #149 → PR #150（merged）
```

## 6. 发布收口

- [x] `docs/todo.md` 的"当前开放任务"小节同步为本文档最终状态（2026-09-06 c32758d）
- [x] 文档基线更新：DeepSeek/MiniMax preset `@3`、Kimi `@4`；Kimi 原生 Responses 位于 migration `0023`，#110 新增的系统事件位于 `0024`。独立 PostgreSQL 测试覆盖迁移，未宣称生产库版本已复核。
- [ ] 打 `v0.1.0` tag：`git tag -a v0.1.0 -m "v0.1.0" && git push origin v0.1.0`
- [ ] GitHub Release 页面创建 v0.1.0 Release，正文包含：功能摘要、验收证据链接（第 2-5 步）、已知未覆盖项（生产数据复验、真实 Provider 性能与 #110 事件中心的部署复验）；OTel #50 已实现

## 明确不在本次验收范围内

- 生产 OTel collector 接收复验（导出代码已由 PR #80 完成）。
- #120 真实 Provider 性能压测；本轮按用户要求纳入了本地性能工具、Mock 基线和故障测试。
- #110 运行时事件中心已按方案 C 由 [PR #183](https://github.com/jianyun8023/my-ai-gateway/pull/183) 合并实现，7/7 项代码验收完成且 Issue 已关闭；本清单此前的 live/生产验收未覆盖其部署 migration、保留清理或生产历史规模查询，不能据本地证据标为通过。

## 阻塞与升级

执行中遇到以下情况必须停下来找人工，不得自行绕过：

1. 需要生产库写权限或真实 Provider 凭据；
2. 本地门禁失败且修复会改动协议、schema 或鉴权语义；
3. Live Smoke 暴露 Gateway 与上游契约漂移（此时按 AGENTS.md 走 Issue → worktree → PR 流程修复）。
