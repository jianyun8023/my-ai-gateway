/**
 * Browser storage key for the Gateway console theme preference.
 *
 * Keep this value stable when changing the UI; it is user state, not a provider
 * or backend contract.
 */
export const STORAGE_KEY_THEME = 'my-ai-gateway-theme';
